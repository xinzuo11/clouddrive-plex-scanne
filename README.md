# clouddrive-plex-scanne
clouddrive-plex-scanne的教程，通过 Webhook 接收 CloudDrive 的文件变更通知，自动触发 Plex 局部扫描，需要 CloudDrive2 会员，除了直接在 CD2 内操作的变更，也支持三方应用调用 CD2 触发的文件变更
创建cd2的webhook的时候需要把base_url = "http://192.168.100.103:5001"中的ip更换成自己的ip
